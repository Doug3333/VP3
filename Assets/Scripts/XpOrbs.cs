using System.Collections;
using System.Collections.Generic;
using System.Runtime.CompilerServices;
using UnityEngine;

public class XpOrbs : MonoBehaviour
{
    /* public BoxCollider2D PickupTrigger;
    public bool pickup;
    //public bool isPickup;                               ???
    //[SerializeField] XpBar;          NOT YET IMPLEMENTED!
    
    [SerializeField] XpOrbs.value;
        public int XpAmount = 2;

    public BoxCollider2D PickupCollider;

    // Start is called before the first frame update
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {
        if (pickup)
        {
            Vector3 bowPositon = transform.position;
            Vector2 mousePos = Camera.main.ScreenToWorldPoint(Input.mousePosition);
            FixedAddressValueTypeAttribute transfer.XpBar;
        }
    }
    */
}
